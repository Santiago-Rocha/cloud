server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Santiago-Rocha/cloud.git
          search-paths: config-data
          username: ${GITHUB_USER}
          password: ${GITHUB_PASSOWRD}
  security:
    user:
      name: root
      password: p4s5wd